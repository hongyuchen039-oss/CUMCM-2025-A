# 当前数学模型

## 题目用人话要求我们做什么
用 1~5 架无人机（FY1~FY5）按受领任务时刻起算的策略飞行并投放烟幕干扰弹，
使得烟幕云团能在来袭导弹（M1/M2/M3，速度 300 m/s 直线飞向假目标）
飞抵真目标（圆柱 r=7 m, h=10 m，中心 (0,200,0)）附近时，
对其"发现真目标"的视线形成尽可能长（可累加、不连续）的有效遮蔽。

## 5 个问题之间的关系
- Q1：参数完全给定，求解一个具体遮蔽时长；用作基线对拍与验证。
- Q2：单弹最优化（FY1 → M1，决策量：航向、速度、投放点、起爆点）。
- Q3：单无人机 3 弹串接（FY1 → M1），将最优策略写入 `result1.xlsx`。
- Q4：3 机各 1 弹协同（FY1,FY2,FY3 → M1），写入 `result2.xlsx`。
- Q5：5 机各至多 3 弹，协同 3 枚导弹 M1,M2,M3，写入 `result3.xlsx`。

建模顺序：Q1（基线）→ Q2（单弹最优）→ Q3（单架多弹串接）
→ Q4（多机协同，单目标）→ Q5（多机协同，多目标）。
每完成一问需在 `RESULTS.md` 记录数值与单位，并按"PLAN/WORKING/VERIFIED/REVIEW/FIX"
流程更新同一任务的 Draft PR。

## 当前可能采用的总体建模路线
仅描述思路，**未冻结**：

1. 统一坐标系：以假目标为原点，xy 为水平面，z 向上；与 PDF §7 一致。
2. 几何模型：
   - 导弹 i 在 t 时刻位置 = 初始位置 + 速度向量 · t，方向恒指假目标；
   - 无人机 j 在 t 时刻位置 = 受领任务后立即按选定航向/速度直线匀速（z 不变）；
   - 烟幕弹脱离后做抛体运动（重力 −z），起爆瞬间变为云团中心；
   - 云团中心以 3 m/s 沿 −z 匀速下沉，云团视为半径 10 m 的球体。
3. 遮蔽判定（待陈虹宇确认两条候选之一）：
   - A. 视线遮蔽：导弹到真目标圆柱面的视线段全部被云团球体覆盖；
   - B. 距离覆盖：云团中心到"导弹→真目标"目标点的距离 ≤ 10 m。
4. 优化目标：累计有效遮蔽时间最长（Q3/Q4/Q5 允许不连续，按原题 §12）。

## 已确认的参数来源
全部参数见 `problem/FACTS.md` §8–§10：
- 导弹速度 300 m/s、初始位置 3 枚、目标指向假目标；
- 无人机初始位置 5 架；
- 无人机速度 70~140 m/s；
- 烟幕弹受重力、云团 3 m/s 下沉、半径 10 m、有效时长 20 s。

## 尚未冻结的假设
- [未冻结] 重力加速度 g 的取值（建议 9.8 m/s²）。
- [未冻结] 烟幕弹脱离无人机瞬间的初速度是否等于无人机当时速度。
- [未冻结] 起爆时刻是否严格 t=0 起即开始下沉。
- [未冻结] Q1 优先使用"云团中心 10 m 覆盖目标"的工程解释，待陈虹宇确认后冻结。

## 局限
- 当前阶段**未建立任何代码或正式结果**。
- 附件结果模板的 xlsx 列结构尚未打开核对。
- Q1 基线尚未跑出，无法验证几何与单位。
