# 当前唯一任务

## 本轮任务
**TASK_001 审核返工 → 等待 GPT 复审**

- 修正官方事实错误（+x/+y、下底面圆心 vs 几何中心、t=0 约定、无风场降级、PDF 页数）；
- 完成三个官方结果模板（result1/2/3.xlsx）表头核验；
- 修正遮蔽判定描述为两层模型（点目标基线 + 完整圆柱正式模型）；
- 明确多弹时间取并集的 [约定]；
- 更新 CLAUDE.md 的"优先于任何临时提示"措辞；
- commit + push 到 task/TASK_001-problem-intake；
- 更新同一个 Draft PR #1 的描述，请求 GPT 复审。

## 本轮明确不做
- 不进入 TASK_002；
- 不编写 Q1 代码；
- 不计算任何数值结果；
- 不修改官方原模板；
- 不创建新分支或新 PR；
- 不合并任何 PR；
- 不直接动 main。

## 本轮完成标准
1. FACTS.md 中每个关键事实带 PDF 页码或附件来源；标记系统严格分 [官]/[推]/[约定]/[假设]。
2. 真目标坐标方向修正为 +y；下底面圆心与几何中心分别记录。
3. 三个结果模板的表头与方向角附注写入 FACTS.md §13。
4. 遮蔽判定改为两层模型（方案 A 点目标 + 方案 B 完整圆柱），不再"二选一"。
5. CLAUDE.md 措辞更新；不创建额外规则文件。
6. 仅修改 8 个文件以内的必要文件。
7. commit + push + 更新 PR 描述。

## 本轮后续（仅当 GPT 复审通过且陈虹宇批准合并后才允许）
- 合并 PR #1 → main。
- 创建新分支 `task/TASK_002-q1-baseline` 启动 Q1 最小可运行基线。
